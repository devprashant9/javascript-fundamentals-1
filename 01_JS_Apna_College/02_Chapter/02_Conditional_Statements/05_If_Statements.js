/*
        if (test expresiion) {
                
        //some code        
        }

        if test expression is true code inside if block is executed. If false the code inside if block never executes
*/

let votingAge_1 = 20;

if (votingAge_1 >= 8) {
    console.log("Eligible to Vote.");
}
// ===============

let votingAge_2 = 16;
if (votingAge_2 > 18) {
    console.log("This Code Section will Never Execute");
}
